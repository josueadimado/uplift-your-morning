# Django and core dependencies
Django>=5.2.8
djangorestframework>=3.16.0
python-decouple>=3.8
django-countries>=7.6.0
requests>=2.31.0
Pillow>=10.0.0
whitenoise>=6.6.0

# Database drivers (install only what you need)
# For PostgreSQL (production):
# psycopg2-binary>=2.9.0
# For MySQL (production alternative):
# mysqlclient>=2.2.0

# For scheduled tasks (optional, for Phase 2)
# celery>=5.3.0
# redis>=5.0.0

# For email sending (optional, for Phase 2)
# sendgrid>=6.10.0
# or
# brevo>=1.0.0

# For SMS notifications (counseling bookings)
# Using FastR API via requests (already included in requirements)
# No additional package needed - uses requests library

# For Google Calendar integration (counseling bookings)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# For export functionality
openpyxl>=3.1.0
reportlab>=4.0.0

# For file storage (optional, for production)
# boto3>=1.28.0  # For AWS S3
# django-storages>=1.14.0

# For Tailwind CSS (optional, can use CDN or external build)
# django-tailwind>=3.8.0

